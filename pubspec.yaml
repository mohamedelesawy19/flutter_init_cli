name: flutter_init_cli
description: A CLI tool to initialize Flutter projects with clean architecture.
version: 1.0.0

environment:
  sdk: ^3.11.5

dependencies:
  args: ^2.4.2
  path: ^1.9.0
  yaml: ^3.1.3
  yaml_edit: ^2.2.0

dev_dependencies:
  lints: ^6.0.0
